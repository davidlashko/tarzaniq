# Test-only dependencies (not needed to run the app).
#   pip install -r requirements.txt -r requirements-dev.txt
# The DOM smoke test also needs Node's jsdom: `npm install` (see package.json).
-r requirements.txt

piexif>=1.1.3   # tests/test_e2e.py writes a deliberately-wrong EXIF date into synthetic JPEGs
